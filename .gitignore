# --- Build output ---------------------------------------------------------
build/
cmake-build-*/
out/
install/

# In-source CMake artifacts (in case someone configures in the source tree)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
Makefile
*.cmake.stamp
compile_commands.json

# --- Compiled binaries / objects ------------------------------------------
ncvista
gen_test
*.o
*.obj
*.a
*.so
*.so.*
*.dylib
*.exe

# --- Generated data -------------------------------------------------------
# Test files produced by gen_test, and netCDF data in general (don't commit
# data sets; use `git add -f` for any example file you deliberately want).
sample.nc
*.nc

# coastlines.bin is a required runtime asset (regenerated via
# scripts/make_coastlines.py) and is intentionally tracked.

# --- Editor / OS cruft ----------------------------------------------------
*~
*.swp
*.swo
.DS_Store
.vscode/
.idea/
*.user

# --- Claude Code local settings -------------------------------------------
.claude

# Python Env
.venv

# --- Sandbox-injected zero-size dot files ---------------------------------
# The dev environment mounts empty placeholder dot files in the work tree;
# they are local-only and must never be committed.
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.gitmodules
.ripgreprc
.mcp.json
.idea
.vscode
